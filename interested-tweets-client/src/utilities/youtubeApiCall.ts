import { GlobalAction } from "@/types/globalStateTypes";
import { Dispatch } from "react";
export async function youtubeApiCall(dispatch: Dispatch<GlobalAction>) {
  try {
  } catch (err) {
  } finally {
  }
}
